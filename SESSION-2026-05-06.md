# GenericKit Session Memory — 2026-05-06

## Architecture Changes
- **Module restructure**: `webrtc/` → `protocols/rtc/`, `video/source_sink.rs`, `capture/generator.rs`, `video/adapter.rs`
- **Trait design**: `VideoSource<F>/VideoSink<F>/AudioSource/AudioSink` with `&self` interior mutability
- **VideoBroadcaster**: IS-A Sink AND Source, `VideoSinkWants` + `aggregate_wants()`
- **CMake**: `GKitCargoExample.cmake` reusable module for Rust examples

## Implemented
- **VideoFrameGenerator**: `FramePattern` trait, `SquarePattern` (move squares + 6×10 OpenCTK font timestamp `YYYY-MM-DD HH:MM:SS.mmm`, scale 3, semi-transparent bg)
- **VideoAdapter**: crop→scale→rate-limit
- **DefaultAudioSource**: 20ms silence frames
- **C FFI**: `gkit_media_video_source_*` (6 functions + callback bridge)
- **C++ RAII**: `gkit::VideoSource` + ImGui example `gkit_media_cpp_example_square_gen`
- **Rust examples**: gkit-media-square-gen (egui), gkit-media-webrtc-loopback (egui, 1280×720 30fps two-PC)
- **Tests**: 17 `test_source_sink` + all existing tests pass

## Bug Fixes
- NV21 U/V swap, ImGui menu `SameLine` relative spacing
- `VideoSource` trait `&mut self` → `&self` (prevent `Arc::get_mut()` panic)
- Font: LSB→MSB, then OpenCTK 6×10 font
- Generator lazy-start thread fix
- `gen` reserved keyword in Rust 2024

## Memory Submodule
- Located at `.agents/GenericKit-Agent-Memory/`
- Configured in `.opencode/opencode.json` via `mem0.storage_dir`

## Pending (from spec)
| Phase | Status |
|-------|--------|
| P0 Source/Sink/Generator | ✅ done |
| P1 webrtc-rs backend | ✅ real impl behind feature gate |
| P2 google_lk backend | ⏳ blocked (build_sys注释) |
| P3 C++ PeerConnection wrapper | ⏳ |
| P4 Callback system | ✅ ICE + track callbacks |
| W3C tests | ✅ 15 new tests (track, ICE, P2P) |

## Latest (2026-05-07)
- P1: real webrtc-rs 0.17.1 backend with sync trait bridging (block_on)
- P4: ICE candidate + state + track callbacks (all &self)
- VideoTrack trait designed (clean naming per W3C)
- create_video_track(source) + set_on_track(cb) API
- OpenH264 codec plan (openh264 0.6)
- webrtc-rs upgraded: 0.11 → 0.17.1
- 53 total tests (stub + real backend both pass)
- Loopback example: gkit API SDP+ICE+state logs, 8 STUN servers
- New design spec: 2026-05-07-p2p-video-pipeline-design.md
